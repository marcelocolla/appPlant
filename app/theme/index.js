import { DefaultTheme, Provider as PaperProvider } from 'react-native-paper'

const theme = {
  ...DefaultTheme,
  colors: {
    appBar: '#4c956c',
    primary: '#222',
    white: '#fff',
    black: '#000',
  },
}

export default theme
