import styled from 'styled-components/native'

import { Snackbar as SnackbarBase } from 'react-native-paper'

export const SnackbarStyled = styled(SnackbarBase)`
  background-color: #333;
`

export const Label = styled.Text`
  color: #fff;
`
